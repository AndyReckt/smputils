name: SMPUtils
version: $version
main: me.andyreckt.smp.SMPUtils
api-version: '1.21'
prefix: SMP
authors: [ AndyReckt ]
description: Utils for the Comick SMP
folia-supported: true

dependencies:
  SERVER:
    Vault:
      load: BEFORE
      required: true
      join-classpath: true